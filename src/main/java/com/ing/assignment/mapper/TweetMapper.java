package com.ing.assignment.mapper;
import com.ing.assignment.model.Tweet;

public final class TweetMapper {

    /**
     * This method maps Input data line to object
     * @param tweet
     * @return object
     */
    /*public static TweetInput map(Tweet tweet) {
        return TweetInput.builder()
                .user(AuthorInfoMapper.map(tweet.getUser()))
                .id(tweet.getId())
                .created_at(tweet.getCreatedAt())
                .text(tweet.getText())
                .build();
    }*/
}
