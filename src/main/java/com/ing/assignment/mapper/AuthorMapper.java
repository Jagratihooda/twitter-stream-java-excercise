package com.ing.assignment.mapper;

import com.ing.assignment.model.Author;
import org.springframework.social.twitter.api.TwitterProfile;

public class AuthorMapper {

    /**
     * This method maps Input data line to object
     * @param tweetProfile
     * @return object
     */
/*    public static Author map(TwitterProfile tweetProfile) {

        return Author.builder().id(tweetProfile.getId())
                .created_at(tweetProfile.getCreatedDate())
                .name(tweetProfile.getName())
                .screen_name(tweetProfile.getScreenName())
                .build();
    }*/
}
